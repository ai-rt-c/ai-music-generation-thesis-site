# Server-only Gemini credential. Never prefix this variable with NEXT_PUBLIC_.
GOOGLE_GENERATIVE_AI_API_KEY=

# Optional model override. The default is gemini-2.5-flash.
THESIS_ASSISTANT_MODEL=gemini-2.5-flash
